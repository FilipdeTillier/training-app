import { StyleSheet } from "@react-pdf/renderer";

export const commonStyles = StyleSheet.create({
  mainHeader: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: "24px",
  },
  mediumHeader: {
    fontSize: "16px",
    marginBottom: 8,
    fontWeight: "bold",
  },
  bold: {
    fontWeight: "bold",
  },
});
